import { makeStyles } from '@material-ui/core/styles';

const useSectionsListStyles = makeStyles((theme) => ({
  sectionBlock: {
    paddingTop: 0,
    paddingBottom: 0,
  }
}))

export default useSectionsListStyles